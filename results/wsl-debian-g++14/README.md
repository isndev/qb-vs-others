# wsl-debian-g++14

WSL2 (Hyper-V guest) — Debian 13, g++ 14.2.0, `-O3 -DNDEBUG`, i9-12900K, pinned to vCPUs 0 and 2,
**5 repetitions + 1 warmup**. Every one of the **84 cells** across the first five `savina-*/`
directories was measured in one quiet session on 2026-09-04 (15:50–16:05 UTC, the Windows side
idle throughout — its own session started at 16:09 UTC), and the candidate qb branch's 20 cells
followed at 16:05 UTC in the same session. The **32 cells** of `savina-fib/` and
`savina-chameneos/` (the two shapes that create actors inside the window, added 2026-09-06) were
measured in a second quiet session that day — the field at 17:40 UTC, shipped qb at 18:13 UTC
after the 3.1.0 build had been rebuilt with the two new adapters, the Windows side idle until
18:14 — bringing this host to 116 cells across seven directories. The **16 cells** of
`savina-bank-transaction/` (the first shape that WAITS for a reply, added 2026-09-07) were
measured in a third quiet session on 2026-09-07, 02:21:41–02:21:46 UTC — the field, then three
qb builds back to back (`qb-branch-perf-coro-scope-local-refcount/`), the Windows side idle
until 02:24 — bringing this host to **132 cells across eight directories**. `run.json` carries `merged_partial_runs`: the field was
brought to single-session provenance framework by framework. qb is the shipped v3.1.0
(`eac739ff`), built clean under `~/qvo/shipped`.

`REPORT.md` beside this file is `tools/report.py`'s render of this directory and
`tools/check-report.py` fails if it drifts. `docs/TUNING.md` §6 is the reading guide, §8 for the
`caf-detached` row and the qb idle-floor experiment, §9 for the four newer shapes.

| directory | what it is |
|---|---|
| `savina-ping-pong/` | **20 cells** — 18 verified + 2 declared `n/a` (`caf-detached` has no spin mode). |
| `savina-counting/`, `savina-thread-ring/`, `savina-fork-join/`, `savina-big/` | **16 cells** each, all verified; `caf-detached` declares itself omitted from these four (`frameworks/caf-detached/CMakeLists.txt`). |
| `savina-fib/`, `savina-chameneos/` | **16 cells** each, all verified, 2026-09-06 (field 17:40 UTC, qb 18:13 UTC), `caf-detached` omitted as above. qb is shipped **3.1.0** and on fib that is the figure of a 3.1.0 nobody tuned: 159 / 205 ms (2c / 1c spin) against CAF's 39 / 69 and a floor of 3.4 / 1.7. Its 3.1.0 logs **9 `LOG_INFO` lines per actor lifetime** (`registerEvent` × 7 — the five default subscriptions and the benchmark's two — plus `New` and `Delete`) at its shipped default `QB_WITH_LOGGING=ON` — measured **515 819 lines, 60.9 MB of `qb.1.log` per repetition** — and nanolog's producer side is inside the window; the candidate demotes them to VERBOSE (`b4baba33`) and measures 7.6 / 11.9 ms in the same session (`qb-branch-perf-dense-table-growth/grid-8362a4b8-session2/`). Measured from an ext4 cwd: on a 9p mount (`/mnt/d`) the exit-time drain of that log takes minutes per repetition — nanolog flushes line by line and every `write()` is a `p9_client_rpc` — which is a real 3.1.0 characteristic and not a runtime hang (gdb: main in `exit()` → `~NanoLogger` → `thread::join`). The caveat string those sixteen qb documents carry says the logger writes "outside the measured window"; that sentence was written for the five static shapes and is FALSE for fib on 3.1.0 — the JSONs are kept as measured and `frameworks/qb/qb_support.h` says the true thing now. Chameneos, 101 actors, logs nothing to speak of inside the window: 15.3 / 12.0 ms (2c / 1c spin), CAF 126 / 123, SObjectizer 154 / 54. |
| `savina-bank-transaction/` | **16 cells**, all verified, 2026-09-07 02:21 UTC, `caf-detached` omitted as above. The first shape that waits for a reply (`benchmarks/savina/bank-transaction.md`): one teller, 1000 accounts, 50 000 transfers each a `qb::ask` awaited from a coroutine. qb is shipped **3.1.0**: 14.71 / 14.58 / 9.21 / 9.31 ms (1c spin / 1c park / 2c spin / 2c park) against CAF 41.5 / 43.0 / 36.6 / 36.6, SObjectizer 19.5 / 20.5 / 25.5 / 27.7 and a floor of 1.10 / 2.59 / 6.30 / 4.63 — 294 ns per transfer on one core for qb, 391 SObjectizer, 830 CAF, 22 the floor. Not a logging figure this time: the `-DQB_WITH_LOGGING=OFF` control in the same session measures 15.80 / 16.28 / 8.99 / 9.75 (`qb-branch-perf-coro-scope-local-refcount/grid-shipped-3.1.0-nolog/`). The shape's one-core profile on qb `develop` found five defects on the ask path (`docs/TUNING.md` §12). |
| `qb-branch-perf-core-hot-path/M-f5c20eeb/` | **the previous candidate** (README.md's grid until `perf/event-pipe-segmented` superseded it): qb at `perf/core-hot-path` `f5c20eeb` (eight commits over 3.1.0, axes I and M included) through the same adapters, all five benchmarks, 5 repetitions — README.md's `framework=qb` grid for this host. `M-f5c20eeb-shipped-3.1.0/` beside it is v3.1.0 measured minutes earlier in the same session, the control. |
| `qb-branch-perf-core-hot-path/L-ba051409/` | the previous candidate (`ba051409`, six commits, before axes I and M), same protocol; kept for the L → M deltas in `docs/TUNING.md` §7. Its thread-ring 1c cells (48 / 50.5) and the current candidate's (37 / 48) are the spread that subsection records for the g++ ring. |
| `qb-branch-perf-core-hot-path/burst-sweep/` | `savina/counting` at one core, spin, with the burst swept 2 k → 4 M messages for the candidate, and 30 k / 1 M for shipped 3.1.0, CAF, SObjectizer and the floor — the instrument behind `docs/TUNING.md` §9.11: the branch dispatches at 8.5 ns at 30 k and 35 at 1 M, the frameworks that allocate per message are flat. 7 repetitions + 2 warmup. Not rendered. |
| `qb-branch-perf-event-pipe-segmented/grid-final/` | **the candidate**: qb at `perf/event-pipe-segmented` `a017b8a5` (two commits over `f5c20eeb`: the segmented pipe, then the slab pool and the staggered segments) through the same adapters, all five benchmarks, **7 repetitions + 2 warmup**, measured 2026-09-05 01:18 UTC in one quiet session with `grid-f5c20eeb/` (01:19 UTC) and `grid-shipped-3.1.0/` (01:19–01:25 UTC, v3.1.0, the control), the Windows side idle throughout — README.md's `framework=qb` grid for this host. `docs/TUNING.md` §9.11 is the reading guide. |
| `qb-branch-perf-event-pipe-segmented/grid-*-pass2/` | the earlier pass of the same three grids (00:49–00:56 UTC), same protocol. Not rendered. |
| `qb-branch-perf-event-pipe-segmented/burst-sweep/` | `savina/counting` at one core, spin, burst swept 2 k → 4 M for the candidate AND for `f5c20eeb` in the same session (01:25 UTC), 30 k / 1 M for shipped 3.1.0, CAF, SObjectizer and the floor — the §9.11 table: 5.9–9.3 ns flat for the candidate against `f5c20eeb`'s 8.8 → 40.1. `burst-sweep-pass2/` is the earlier pass. 7 + 2. Not rendered. |
| `qb-branch-perf-event-pipe-segmented/burst-sweep-prehoist/`, `grid-slabs-prehoist/` | the A/B that shaped the branch, 2026-09-04 20:48–23:58 UTC: `pass1-malloc-segments/` is the segmented pipe with `malloc`-laid segments (copies nothing, still 15 640 minor faults at 1 M), `pass2-slabs/` and `pass2-slabs-rerun/` the slab pool before the width hoist and the stagger — and `grid-slabs-prehoist/` is the document with the defect in it: `big` at one core at 107 ms per repetition against 52, the 4K aliasing §9.11 explains. Not rendered. |
| `qb-branch-perf-park-in-ev-loop/` | **axis N** (`perf/park-in-ev-loop`, branched from `develop` `f0da4e32`, the control): `parked-io-wake.log` is the qb-only probe `tools/probes/parked-io-wake.cpp` over 12 latency × gap cells, two interleaved passes, 2000 rounds — a parked core with a readable socket under it answered in **923 / 9961 µs** at `setLatency` 1 / 10 ms and answers in **~31 µs** on the branch (the probe pinning core and client itself; the earlier unpinned grid, same p50s, is kept beside it for what its tails taught); `ab/` is the ping-pong regression gate, 4 cells × 3 interleaved reps, no cell moved. 2026-09-06 02:19–03:22 UTC, quiet sessions. `docs/TUNING.md` §10. Not rendered. |
| `qb-branch-perf-dense-table-growth/` | **the qb branch fib and chameneos produced** (`perf/dense-table-growth`, three commits over `develop` `22c9bf6e`), 2026-09-06: `grid-3ea9a2ae/` and `grid-8362a4b8/` are the full field at each step of the branch (32 cells each, **9 + 2**, 17:02–17:23 UTC), `grid-8362a4b8-session2/` the branch's 8 qb cells at this host's protocol (5 + 1) in the SAME session as the shipped-3.1.0 control above (18:13 UTC) — the shipped-vs-candidate pair the protocol asks for. fib measured **43 s** per repetition on `develop` before the branch (the dense router's table grew by one slot per new actor — O(n²), unreleased), shipped 3.1.0 measures **159 / 205 ms** (2c / 1c spin), the branch **7.6 / 11.9** — 21× / 17× — against CAF's 39 / 69 and a raw-thread floor of 3.4 / 1.7; chameneos 15.3 / 12.0 → 11.1 / 6.5. Its own README carries the per-commit chain and the field. Not rendered: the seven-shape 3.2.0 grid is where the candidate joins the tables. |
| `qb-branch-perf-default-event-registry/` | **the A/B for QB-174** (`perf/default-event-registry` `dc1ac56e` over `develop` `a6663641`: the five default events dispatch through the actor registry), 2026-09-06 21:06:00–21:06:04 UTC: candidate / control / candidate, **8 qb-only cells each** (fib × chameneos × 4 configurations, 9 + 2), the Windows side idle. fib p50 2c 6.96 → 5.21 / 5.31 ms, 1c 10.98 → 8.62 / 8.48; chameneos inside its spread. Its own README carries the chain. Not rendered. |
| `qb-branch-perf-ask-slot-table/` | **the A/B for QB-178** (`perf/ask-slot-table`: `dfa303ec` the slot table `[core:16][generation:26][slot:22]`, `82ea03a8` take-binds / `finish()`-once, over `develop` `ba9b1a81`), 2026-09-07, two sessions — A 06:01:55–06:03:35 UTC for `dfa303ec`, B 06:18:10–06:19:50 UTC for the head — the Windows side idle: candidate / control / candidate, **4 qb-only cells each** (bank-transaction × 4 configurations, 9 + 2), then the two `dev/bench` ask binaries cand/ctl × 3. Head: bank-transaction 1c-spin p50 7.70 → 7.21 / 7.25 ms (**−6 %**), 1c-park 7.58 → 7.30 / 7.24 (**−4 %**), 2c inside its spread, round trips flat; the `perf` chain behind it (registry 12.8 → 8.4 → 4.36 % of the core) is in its README. Not rendered. |
| `qb-branch-perf-coro-scope-local-refcount/` | **the three qb controls bank-transaction asked for**, in the SAME session as the field above (02:21:43–02:21:46 UTC, 5 + 1, qb-only builds): `grid-base-203cfb56/` is `develop` `203cfb56`, the commit the work branched from — **9.40 / 9.44 / 5.09 / 5.02 ms**; `grid-9814c2a1/` is `develop` `9814c2a1`, the five fixes — **8.06 / 8.80 / 4.56 / 4.77** (−14 % / −10 % at 1c / 2c spin; 3.1.0 → `9814c2a1` 1.8× / 2.0×); `grid-shipped-3.1.0-nolog/` is shipped 3.1.0 with `-DQB_WITH_LOGGING=OFF` — 15.80 / 16.28 / 8.99 / 9.75, the control that says 3.1.0's figure is not a logging figure. 4 cells each, all verified. `docs/TUNING.md` §12 carries the chain and what remains (the ask registry, ≈10–12 % of the core). Not rendered. |
| `qb-branch-perf-core-hot-path/` (the loose files) | earlier side experiments at `39992047`, NOT rendered by the report: the four ping-pong cells with the shipped build beside them, the axis-K A/B, the `idlespin*` files (§8.2). |

**Caveat that applies to every `2c-park` cell of a benchmark that crosses a core per message:**
a futex wake of a thread parked on another vCPU costs ~12 µs under WSL2, so the raw
condition-variable floor is **25.47 µs per ping-pong round trip** and **13.01 µs per ring hop**
(`baseline__2c-park` in each), and any framework that truly parks across cores measures the
hypervisor, not itself: qb 3.1.0 26.78 µs / 13.41 µs, SObjectizer 26.53 µs on ping-pong,
`caf-detached` in its slow mode 25.81 µs. The pooled `caf` row (290 / 141 ns) is below that floor
because it never crosses a core. counting, fork-join and big do not collapse — their consumers
never run dry — and every `1c-*` and `2c-spin` cell is unaffected.

**`caf-detached__2c-park` is bimodal** — 1 of 5 repetitions at ~3.6 µs, 4 at ~25.8 µs in this
document, 3 of 5 fast on the previous session's — and the report marks it so instead of quoting
its median. See `frameworks/caf-detached/README.md`.
