# desktop-b67osn6-win-msvc

Windows 11 (10.0.26100), MSVC 19.51 (VS 2026), `/O2 /DNDEBUG`, i9-12900K, pinned to CPUs 0 and 2
(two P-cores), **9 repetitions + 2 warmup**. Every one of the **84 cells** across the first five
`savina-*/` directories was measured in ONE quiet session on 2026-09-04 (16:09–16:18 UTC) — no
build, no test suite and no WSL measurement running anywhere on the host (the WSL2 session ended
at 16:05 UTC) — and the candidate qb branch's 20 cells followed at 16:17–16:18 UTC in the same
session, so shipped and branch share their provenance. The **32 cells** of `savina-fib/` and
`savina-chameneos/` (the two shapes that create actors inside the window, added 2026-09-06) were
measured the same way in one quiet session on 2026-09-06, 18:14–18:16 UTC, after the WSL2
session had ended at 18:13 — the field, shipped qb 3.1.0 (`build/shipped-win`, rebuilt with the
two new adapters) and the candidate branch's eight qb cells back to back — bringing this host to
116 cells across seven directories. The **16 cells** of `savina-bank-transaction/` (the first
shape that WAITS for a reply, added 2026-09-07) were measured the same way in one quiet session
on 2026-09-07, 02:24:23–02:24:36 UTC, after the WSL2 session had ended at 02:21 — the field,
shipped qb 3.1.0 (`build/shipped-win`, rebuilt with the new adapter) and two `develop` builds back
to back (`qb-branch-perf-coro-scope-local-refcount/`) — bringing this host to **132 cells across
eight directories** — and the **96 qb cells** of `qb-branch-develop/` (2026-09-07, 08:21:40–08:26:18 UTC) are the 3.2.0 candidate grid, `develop` `f8eba11d` against shipped 3.1.0 on all eight shapes, which is what README.md renders for this host since that day. `run.json` records `merged_partial_runs`
because the session was driven as `--only <framework>` runs that `tools/run.py` merged; it
refuses a merge whose host, CPU set or repetition count differs.

`REPORT.md` beside this file is `tools/report.py`'s render of this directory and
`tools/check-report.py` fails if it drifts from the JSON. `docs/TUNING.md` is the reading guide:
§1.1 for CAF's two columns, §5/§7 for qb's park cell, §8 for `caf-detached` and the idle-floor
experiment, §9 for what the four newer shapes say about qb.

| directory | what |
|---|---|
| `savina-ping-pong/` | 4 configurations × {`baseline`, `qb` 3.1.0, `caf` 1.1.0, `caf-detached`, `sobjectizer` 5.8.5.1}: **18 verified + 2 `n/a`** (`caf-detached` has no spin mode; harness exit 3, reason in the document). `qb` is the shipped v3.1.0 (`eac739ff`, `git archive`d and built apart from the branch). The `caf-detached` 2c-park cell is **bimodal** (4 of 9 repetitions at ~1.0–1.5 µs, 5 at ~10.6 µs) and the report says so; so is shipped qb's 2c-park on `thread-ring` below. |
| `savina-counting/`, `savina-thread-ring/`, `savina-fork-join/`, `savina-big/` | the same 4 configurations × {`baseline`, `qb`, `caf`, `sobjectizer`}: **16 verified** each, no `n/a` (`caf-detached` declares itself omitted from these four in its `CMakeLists.txt`). Shipped qb's `thread-ring` 2c-park is **bimodal** — 2 of 9 at ~0.5–0.7 µs per hop, 7 at 2.0–3.6 µs — the §5 collapse on a ring. |
| `savina-fib/`, `savina-chameneos/` | the same 4 configurations × 4 frameworks: **16 verified** each, 2026-09-06 18:14–18:16 UTC, `caf-detached` omitted as above. qb is shipped **3.1.0** and on fib that is the figure of a 3.1.0 nobody tuned: **459 / 558 ms** (2c / 1c spin) against CAF's 53 / 85 and a floor of 4.0 / 3.7. Its 3.1.0 logs **9 `LOG_INFO` lines per actor lifetime** (`registerEvent` × 7 — the five default subscriptions and the benchmark's two — plus `New` and `Delete`) at its shipped default `QB_WITH_LOGGING=ON` — 515 819 lines, 60.9 MB of `qb.1.log` per repetition, measured on the WSL2 host, the same code — and nanolog's producer side is inside the window, which is why Windows pays more than Linux here (the CRT heap and a per-line `WriteFile` against glibc and ext4). The candidate demotes those lines to VERBOSE (`b4baba33`) and measures **10.5 / 17.2 ms** in the same session (`qb-branch-perf-dense-table-growth/grid-8362a4b8-session2/`) — 44× / 32×. The caveat string those sixteen qb documents carry says the logger writes "outside the measured window"; it was written for the five static shapes and is FALSE for fib on 3.1.0 — the JSONs are kept as measured and `frameworks/qb/qb_support.h` says the true thing now. Chameneos, 101 actors, logs nothing to speak of inside the window: 20.8 / 13.2 ms (2c / 1c spin), CAF 217 / 215, SObjectizer 260 / 76. |
| `savina-bank-transaction/` | the same 4 configurations × 4 frameworks: **16 verified**, 2026-09-07 02:24:23–02:24:33 UTC, `caf-detached` omitted as above. The first shape that waits for a reply (`benchmarks/savina/bank-transaction.md`): one teller, 1000 accounts, 50 000 transfers each a `qb::ask` awaited from a coroutine. qb is shipped **3.1.0**: **25.48 / 25.45 / 29.20 / 33.45 ms** (1c spin / 1c park / 2c spin / 2c park) against CAF 57.8 / 58.0 / 57.5 / 58.1, SObjectizer 29.6 / 31.5 / 38.0 / 44.4 and a floor of 3.47 / 3.92 / 32.2 / 7.25. Three of the `2c` cells that cross a core per transfer are WIDE: the floor's 2c-spin spans 20.0–62.2 ms over nine repetitions (IQR 29.5), shipped qb's 2c-spin 24.3–49.6 (IQR 13.0) and its 2c-park 19.2–42.1 (IQR 18.5); the report's bimodality rule does not fire on them and they are quoted with their spread. The `develop` cells of the same session (below) do not show it. |
| `qb-branch-perf-core-hot-path/M-f5c20eeb/` | **the previous candidate** (README.md's grid until `perf/event-pipe-segmented` superseded it): qb at `perf/core-hot-path` `f5c20eeb` (eight commits over 3.1.0, axes I and M included) through the unmodified adapters, all five benchmarks, 9 repetitions — the `framework=qb` grid README.md carries and `check-report.py` verifies. `M-f5c20eeb-shipped-3.1.0/` beside it is v3.1.0 measured minutes earlier in the same session, the control. |
| `qb-branch-perf-core-hot-path/L-ba051409/` | the previous candidate (`ba051409`, six commits, before axes I and M), same protocol; kept so the L → M deltas in `docs/TUNING.md` §7 can be re-derived. |
| `qb-branch-perf-core-hot-path/ab-axis-I/`, `ab-axis-IM/` | the interleaved A/B documents behind §7 "Axes I and M": `ab-axis-I/{L,I}` is axis I alone against L (the counting-2c regression is in `I/savina-counting/`), `ab-axis-IM/{L1,IM1,L2,IM2}` the four-pass L against I + M. 9 repetitions each. Not rendered. |
| `qb-branch-perf-core-hot-path/burst-sweep/` | `savina/counting` at one core, spin, with the burst swept 2 k → 4 M messages for the candidate, and 30 k / 1 M for shipped 3.1.0, CAF, SObjectizer and the floor — the instrument behind `docs/TUNING.md` §9.11 and §9.12. 7 repetitions + 2 warmup. Not rendered. |
| `qb-branch-perf-event-pipe-segmented/grid-final/` | **the previous candidate** (README.md's grid until `develop` `f8eba11d` superseded it on 2026-09-07): qb at `perf/event-pipe-segmented` `a017b8a5` (two commits over `f5c20eeb`: the segmented pipe, then the slab pool and the staggered segments) through the unmodified adapters, all five benchmarks, **7 repetitions + 2 warmup**, measured 2026-09-05 01:16 UTC in one quiet session with `grid-f5c20eeb/` (01:07 UTC, the previous candidate rebuilt from the same tree) and `grid-shipped-3.1.0/` (01:07–01:08 UTC, v3.1.0, the control) — README.md's `framework=qb` grid for this host until `qb-branch-develop/` replaced it. `docs/TUNING.md` §9.11 is the reading guide: the two `2c-spin` cells of ping-pong and thread-ring are bimodal within a launch here and the census, not the grid, is the figure to quote for them. |
| `qb-branch-perf-event-pipe-segmented/grid-*-pass<N>/` | the three earlier passes of the same three grids (00:46–00:48, 01:03–01:05 and 01:07 UTC), same protocol, kept so the per-cell spread across passes can be re-derived (branch ping-pong 2c-spin medians 274–319 ns against `f5c20eeb`'s 258–263 over four passes — the finding §9.11 takes to the census). Not rendered. |
| `qb-branch-perf-event-pipe-segmented/launch-census/` | the candidate (`a017b8a5`, `build/final/`) against `f5c20eeb` on the eight cells of ping-pong and thread-ring (1c/2c × spin/park): **12 launches interleaved, 3 + 1 each**, pinned `0,2`, `tools/launch-census.py`, measured 2026-09-06 ~01:45 UTC with the host otherwise idle, `census.log` beside the 192 documents — the same protocol as the macOS host's. Every cell's two distributions **overlap**: ping-pong 1c-spin 82.2 vs 82.5, 1c-park 84.5 vs 83.6, 2c-spin 269.8 vs 265.9, 2c-park 271.9 vs 271.3; thread-ring 1c-spin 49.0 vs 47.8, 1c-park 48.0 vs 47.4, 2c-spin 133.3 vs 138.8, 2c-park 142.9 vs 146.1 (median-of-medians, ns). The figure to quote for the two bimodal `2c-spin` cells of the grid — and note the absolute level of this session sits 5–8 % above the earlier scratch censuses (ping-pong 2c-spin ~250, 1c ~77), on BOTH sides alike, which is what interleaving is for: the host's evening state moves the pair, not the difference. |
| `qb-branch-perf-event-pipe-segmented/grid-order-census/` | the grid's own launcher (`tools/run.py`, 7 + 2, same cell order) over a `bin/` holding ONLY ping-pong and thread-ring, four interleaved rounds per binary (`f5c20eeb-<N>/`, `final-<N>/`, 01:27–01:29 UTC), with `census.log` — the instrument that separates the sequence from the build for the two bimodal `2c-spin` cells: ring 129.5 → 122.8 ns, ping-pong 238.3 → 251.8 with the launches overlapping. Not rendered. |
| `qb-branch-perf-event-pipe-segmented/burst-sweep/` | `savina/counting` at one core, spin, burst swept 2 k → 4 M for the candidate AND for `f5c20eeb` in the same session (01:16 UTC), 30 k / 1 M for shipped 3.1.0, CAF, SObjectizer and the floor — the §9.11 table: 6.6–10.4 ns flat for the candidate against `f5c20eeb`'s 19–34. `burst-sweep-pass1/`, `-pass2/` are the two earlier passes. 7 + 2. Not rendered. |
| `qb-branch-perf-park-in-ev-loop/` | **axis N** (`perf/park-in-ev-loop`, branched from `develop` `f0da4e32`, the control): the parked-io-wake probe grid — a core that owns a TCP listener and has parked answered a readable socket only when its `latency` expired, and on Windows MSVC's `wait_for` rounds that to the 15.6 ms tick: control p50 **1.2 ms at latency 100 µs, 2.2–2.8 ms at 1 ms, 15.6 ms at 10 ms** (`gap=200us`), candidate **24–27 µs** — plus the ping-pong A/B (four cells × 3 interleaved reps) showing no move on a benchmark that has no io watcher. 2026-09-06 03:02–03:27 UTC, quiet host, the probe pinning core and client itself. `docs/TUNING.md` §10. Not rendered. |
| `qb-branch-perf-dense-table-growth/` | **the qb branch fib and chameneos produced** (`perf/dense-table-growth`, three commits over `develop` `22c9bf6e`), 2026-09-06: `grid-3ea9a2ae/` and `grid-8362a4b8/` are the full field at each step of the branch (32 cells each, **9 + 2**, 17:01–17:24 UTC), `grid-8362a4b8-session2/` the branch's 8 qb cells (9 + 2) in the SAME session as the shipped-3.1.0 control above (18:16 UTC) — the shipped-vs-candidate pair the protocol asks for. fib measured **43 s** per repetition on `develop` before the branch (the dense router's table grew by one slot per new actor — O(n²), unreleased), shipped 3.1.0 measures **459 / 558 ms** (2c / 1c spin), the branch **10.5 / 17.2** — 44× / 32× — against CAF's 53 / 85 and a raw-thread floor of 4.0 / 3.7; chameneos 20.8 / 13.2 → 12.8 / 7.6. Its own README carries the per-commit chain and the field. Not rendered: `qb-branch-develop/` is where the candidate joins the tables. |
| `qb-branch-perf-default-event-registry/` | **the A/B for QB-174** (`perf/default-event-registry` `dc1ac56e` over `develop` `a6663641`: the five default events dispatch through the actor registry), 2026-09-06 21:06:26–21:06:30 UTC, after the WSL2 grids had ended: candidate / control / candidate, **8 qb-only cells each** (fib × chameneos × 4 configurations, 9 + 2). fib p50 2c 10.18 → 7.10 / 7.03 ms, 1c 15.73 → 11.03 / 11.48; chameneos inside its spread. Its own README carries the chain. Not rendered. |
| `qb-branch-perf-ask-slot-table/` | **the A/B for QB-178** (`perf/ask-slot-table` `82ea03a8` over `develop` `ba9b1a81`: the pending-ask registry is a slot table the correlation id indexes, take binds and `finish()` runs once), 2026-09-07 06:28:27–06:28:29 UTC, after the WSL2 session B had ended: candidate / control / candidate, **4 qb-only cells each** (bank-transaction × 4 configurations, 9 + 2), then the two `dev/bench` ask binaries cand/ctl × 3 (`bench/`, 06:32:59–06:34:40 UTC). **Inside the spread on every cell on MSVC** — bank-transaction 1c-spin p50 12.22 → 11.99 / 12.07 ms with the control's minimum the lowest of the three, round trips +1.5 %, scatter −0.5 % — where WSL2 measured −6 % / −4 % on the 1c pair; its own README carries the reading. Not rendered. |
| `qb-branch-perf-coro-scope-local-refcount/` | **the two qb controls bank-transaction asked for**, in the SAME session as the field above (02:24:34–02:24:36 UTC, 9 + 2, qb-only builds): `grid-base-203cfb56/` is `develop` `203cfb56`, the commit the work branched from — **13.65 / 13.86 / 7.97 / 7.78 ms**; `grid-9814c2a1/` is `develop` `9814c2a1`, the five fixes — **12.91 / 12.82 / 7.57 / 7.56** (−5 % / −5 % at 1c / 2c spin; 3.1.0 → `9814c2a1` 2.0× / 3.9×, the last over the wide shipped cell). 4 cells each, all verified, IQR 0.2 on the 2c cells where shipped 3.1.0 spreads 13–18. The WSL2 host adds the `QB_WITH_LOGGING=OFF` control; `docs/TUNING.md` §12 carries the chain. Not rendered. |
| `qb-branch-develop/` | **the 3.2.0 candidate grid** — qb `develop` `f8eba11d` (29 commits over v3.1.0, every perf branch above merged) through the unmodified adapters on **all eight shapes**, 9 + 2, candidate / shipped 3.1.0 / candidate in ONE quiet session on 2026-09-07, **08:21:40–08:26:18 UTC** (`build/final` rebuilt at `f8eba11d` against `build/shipped-win`; the WSL2 side idle, its own session 08:45–08:54 UTC afterwards): **96 qb cells**, 32 / 32 verified in each of the three grids — `grid-f8eba11d/` is README.md's `framework=qb` grid for this host and `check-report.py` verifies it. All 32 cells faster than shipped, the fastest framework in every cell, below the floor in 11 of the 16 two-core cells; both 2c-park collapses gone (7.79 µs → 274 ns, 2.62 µs → 146 ns), fib 44 × / 70 ×, bank −50 / −79 %. Its README.md carries the table and the residuals; `docs/TUNING.md` §13 the two-host reading. |
| `qb-branch-perf-loop-clock-on-demand/` | **the A/B for QB-180** (`perf/loop-clock-on-demand` `0f7994e6` over `develop` `f8eba11d`: the tick phase no longer builds a `LoopEvent` — and reads the clock — on a core with no registered callback, and every idle pass reads the idle clock in every latency mode; the WSL2 directory carries the profile, the guard-only regression and the variant censuses that shaped the fix), 2026-09-07 **10:17:17–10:23:15 UTC** in one quiet session with the WSL2 side idle: candidate / control / shipped 3.1.0 / candidate on ping-pong, counting and thread-ring, 12 qb-only cells each (9 + 2), then the four core `dev/bench` binaries cand/ctl × 3 (`bench/`) and a ten-launch census (`census/`). ping-pong 1c-spin p50 **80.7 → 41.6 ns** (**−48 %**; shipped 113.6), thread-ring 1c-spin **44.7 → 22.9** (**−49 %**), the 2c cells and the counting cells inside their spread (both builds bimodal on counting, §9.11); `Mono_PingPong` 117.8 → 76.7 ns, the one-core pipeline 79.1 → 43.3 ns per delivery, ask round trips −6 / −7 %. `docs/TUNING.md` §14. Not rendered. |
| `qb-branch-perf-core-hot-path/` (the loose files) | earlier side experiments on the branch at `39992047`: the four ping-pong cells with the shipped build beside them, the axis-K A/B and the `idlespin*` floor experiment. Not rendered by `report.py` (its own README says why). |
| `caf-spin-sweep/` | side experiment: the ten-point sweep of CAF's two work-stealing knobs that established `wait=1` ≡ `wait=0` for CAF. Not rendered. |

Toolchain provenance is in every document's `env` object; `tools/report.py` prints it once per
report and refuses to render two documents for one cell.
